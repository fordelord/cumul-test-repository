export const _handleError = (errorMessage, parameters) => {
    let message = 'The dashboard component encountered an error';
    if (typeof (parameters === null || parameters === void 0 ? void 0 : parameters.dashboardId) === 'string') {
        message += ` for dashboard with id ${parameters.dashboardId}`;
    }
    if (typeof (parameters === null || parameters === void 0 ? void 0 : parameters.dashboardSlug) === 'string') {
        message += ` for dashboard with slug ${parameters.dashboardSlug}`;
    }
    if (typeof (parameters === null || parameters === void 0 ? void 0 : parameters.container) === 'string') {
        message += ` in container ${parameters.container}`;
    }
    if (errorMessage) {
        message += `:\n${errorMessage}`;
        console.error(message);
    }
};
