import { ScreenMode } from '../models/common.model';
export interface ScreenModeExtents {
    mode: Exclude<ScreenMode, 'auto'>;
    height?: number;
    width: number[];
}
export declare const EXTENTS: ScreenModeExtents[];
